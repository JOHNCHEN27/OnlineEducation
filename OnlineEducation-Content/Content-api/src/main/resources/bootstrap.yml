server:
  servlet:
    context-path: /content #接口的根路径 --指定所有接口的第一个路径
  port: 64040
#微服务配置
spring:
  application:
    name: content-api #微服务名称
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.113.185.5:3306/online_education_content?serverTimezone=UTC&userUnicode=true&useSSL=false
    username: root
    password: root
  # 日志文件配置路径
  logging:
    config: classpath:log4j2-dev.xml

#配置swagger生成接口文档
swagger:
  title: "在线教育内容管理系统"
  description: "内容系统管理系统对课程相关信息进行管理"
  base-package: com.lncanswer.content   #扫描包的路径
  enabled: true #开启swagger功能
  version: 1.0.0
