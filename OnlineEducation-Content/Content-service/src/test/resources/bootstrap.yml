#微服务配置 将其放到测试单元的resource下 进行单元测试使用
spring:
  application:
    name: content-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.101.65:3306/online_education_content?serverTimezone=UTC&userUnicode=true&useSSL=false
    username: root
    password: mysql
    type: com.alibaba.druid.pool.DruidDataSource
  # 日志文件配置路径

  logging:
    config: classpath:log4j2-dev.xml