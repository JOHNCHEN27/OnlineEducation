#微服务配置 将其放到测试单元的resource下 进行单元测试使用
spring:
  application:
    name: content-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver  #此处配置的是aliyun服务器
    url: jdbc:mysql://47.113.185.5:3306/online_education_content?serverTimezone=UTC&userUnicode=true&useSSL=false
    username: root
    password: root
  redis:
    host: localhost
    password: root
    port: 6379

  # 日志文件配置路径

  logging:
    config: classpath:log4j2-dev.xml